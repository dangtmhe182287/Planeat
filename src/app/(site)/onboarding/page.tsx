import Onboarding from "@/components/Onboarding";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Complete Your Profile | Planeat",
};

const OnboardingPage = () => {
  return <Onboarding />;
};

export default OnboardingPage;